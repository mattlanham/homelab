apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: pgvector-tunnel
  namespace: pgvector
subjects:
  - name: pgvector # Name of our service 
tunnelRef:
  kind: ClusterTunnel
  name: k3s-cluster-tunnel
